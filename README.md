sam_sasi_tracking.py : image segmantation algorithm (cannot be used for videos) 


sam2sasi.py : video object segmentation algorithm for cpu


last.py : video object segmentation algorithm for cpu, has additional object detection logic which gets the lable of objects and displays the order in which the objects are placed inside the main base. ( Improvements needed: uses bounding box for segmented objects, the base logic is correct but not displayed)



final_unsure.py : unchecked code(with all changes)


git ref: https://github.com/yas-sim/openvino-segment-anything-interactive-demo/tree/main

git ref : https://github.com/facebookresearch/sam2
